# DockRadar — Environment Configuration
# Copy this file to .env and fill in your values.

# ── User and Group IDs ───────────────────────────────────
PUID=0
PGID=0

# ── Scheduler ────────────────────────────────────────────
# How often to run automatic scans (in hours)
SCAN_INTERVAL_HOURS=6

# ── SMTP / Email Notifications ───────────────────────────
# Leave blank to disable email notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASSWORD=your_app_password

EMAIL_FROM=dockradar@example.com
EMAIL_TO=admin@example.com

# ── Application ──────────────────────────────────────────
HOST=0.0.0.0
PORT=8080
LOG_FILE=dockradar.log

# ── Registry cache TTL (seconds) ─────────────────────────
REGISTRY_CACHE_TTL=300

# ── Docker Host ─────────────────────────────────────────
# DOCKER_HOST=tcp://192.168.29.25:2375
# DOCKER_HOST=ssh://ankit@192.168.29.25
